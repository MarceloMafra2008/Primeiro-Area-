#include <stdio.h>
int main()
{
    float altura, peso_ideal;
    printf("digite a altura do usuario:");
    scanf("%f", &altura);

    peso_ideal = (72.7 * altura) - 58;
    printf("peso ideal do usuario:%.2f", peso_ideal);
}
//formula do peso ideal = (72.7 * altura) - 58
